name: 'Standardized Risk Assessment'
description: 'Calculates risk score for pull requests based on AI analysis and automated metrics'
inputs:
  github-token:
    description: 'GitHub token for API access'
    required: true
  config:
    description: 'Base64 encoded risk questions configuration'
    required: false
outputs:
  risk-score:
    description: 'Calculated risk score'
  risk-tier:
    description: 'Risk tier'
  status:
    description: 'Check status'
  fallback:
    description: 'Whether fallback model was used'
runs:
  using: 'node20'
  main: 'dist/index.js'
