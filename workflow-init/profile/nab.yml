overlays: &defaultOverlays
  dev: [ ]
  test: [ ]
  qa: [ ]
  prod:
    - nab-dev

ecr: &defaultECR
  registry: "569345997196"
  region: us-east-1
  repository-prefix: ''

parameters: &defaultParameters
  java-version: 11
  # bitbucket, bitbucket-fake, github, github-fake
  kustomize: bitbucket-fake

component:
  ai-data-sink:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket-fake
    ecr:
      <<: *defaultECR
      repository: hawk-ai-data-sink
    kustomize:
      path: plattform/ai/ai-data-sink
      overlays: *defaultOverlays

  audit-trail:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-audit-trail
    kustomize:
      path: plattform/utility/audit-trail
      overlays: *defaultOverlays

  audit-trail2:
    parameters: *defaultParameters
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-audit-trail
    kustomize:
      overlays: *defaultOverlays
    modules:
      app:
        parameters: *defaultParameters
        ecr:
          <<: *defaultECR
          repository: hawk-audit-trail
          repository-prefix: hawk-audit-trail
        kustomize:
          path: plattform/utility/audit-trail
          overlays: *defaultOverlays      

  backend:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
      msg:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-case-manager-backend-msg
          repository-prefix: hawk-
        kustomize:
          path: plattform/case-manager/backend
          overlays: *defaultOverlays
      rest:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-case-manager-backend-rest
          repository-prefix: hawk-
        kustomize:
          path: plattform/case-manager/backend
          overlays: *defaultOverlays
      grouping:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-backend-grouping
          repository-prefix: hawk-
        kustomize:
          path: plattform/case-manager/backend
          overlays: *defaultOverlays
      search:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-backend-search
          repository-prefix: hawk-
        kustomize:
          path: plattform/case-manager/backend
          overlays: *defaultOverlays

  blacklist:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-hawkai-blacklist-msg
      repository-prefix: hawk-
    kustomize:
      path: plattform/processing/features/blacklist
      overlays: *defaultOverlays

  case-sink:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-case-sink
      repository-prefix: hawk-
    kustomize:
      path: plattform/processing/case-sink
      overlays: *defaultOverlays

  configuration:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
      java-version: 17
    ecr:
      <<: *defaultECR
      repository: hawk-configuration
      repository-prefix: hawk-
    kustomize:
      path: plattform/utility/configuration
      overlays: *defaultOverlays

  customer-risk-rating:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
      app:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-customer-risk-rating
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/customer-risk-rating
      crr-risk-factor-sar:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-crr-risk-factor-sar
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/crr-risk-factors/crr-risk-factor-sar
      crr-risk-factor-expected-volume:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-crr-risk-factor-expected-volume
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/crr-risk-factors/crr-risk-factor-expected-volume
      crr-risk-factor-payment-means:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-crr-risk-factor-payment-means
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/crr-risk-factors/crr-risk-factor-payment-means

  customer-risk-rating-events-sink:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-customer-risk-rating-events-sink
    kustomize:
      path: plattform/processing/customer-risk-rating-events-sink
      overlays: *defaultOverlays

  customer-risk-rating-decision-engine:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-customer-risk-rating-decision-engine
    kustomize:
      path: plattform/processing/customer-risk-rating-decision-engine
      overlays: *defaultOverlays

  decision-engine:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
      opening:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-decision-engine-msg
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/decision-engine
      autoclosing:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-decision-engine-auto-closing
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/decision-engine

  document-storage:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-document-storage-api
      repository-prefix: hawk-
    kustomize:
      path: plattform/utility/document-storage
      overlays: *defaultOverlays

  entity-resolution:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket-fake
    ecr:
      <<: *defaultECR
      repository: hawk-entity-resolution
      repository-prefix: hawk-
    kustomize:
      path: plattform/ai/entity-resolution
      overlays: *defaultOverlays

  gateway:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
      engine:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
      msg:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-gateway-msg
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/gateway
      batch:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-gateway-batch
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/gateway
      rest:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-gateway
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/gateway
      activemq:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket

  features:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
      java-version: 17
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-features
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
      countryrisk:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-countryrisk
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/countryrisk
      volumerisk:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-volumerisk
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/volumerisk
      pepsanction-lookup-match-analyzer:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-pepsanction-lookup-match-analyzer
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction-lookup-match-analyzer
      simple:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-simple
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/simple
      assessment-engine:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-assessment-engine
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/assessment-engine
      passthrough:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-passthrough
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/passthrough
      smurfing:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-smurfing
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/smurfing
      roundamount:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-roundamount
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/roundamount
      round-amount-ratio-increase-decrease:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-round-amount-ratio-increase-decrease
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/round-amount-ratio-increase-decrease
      unusualfrequency:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-unusualfrequency
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/unusualfrequency
      dormantaccount:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-dormantaccount
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/dormantaccount
      crossbordervolume:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-crossbordervolume
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/crossbordervolume
      volumeincreasedecrease:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-volumeincreasedecrease
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/volumeincreasedecrease
      suspiciousavgticketsize:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-suspiciousavgticketsize
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/suspiciousavgticketsize
      account-based-frequency-increase-decrease:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-account-based-frequency-increase-decrease
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/account-based-frequency-increase-decrease
      volumeriskv2:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-volumeriskv2
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/volumeriskv2
      countryriskv2:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-countryriskv2
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/countryriskv2
      passthroughv2:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-passthroughv2
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/passthroughv2
      passthroughv3:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-passthroughv3
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/passthroughv3
      counterparty-volume:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-counterparty-volume
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/counterparty-volume

  features2:
    parameters:
      <<: *defaultParameters
      java-version: 17
      kustomize: github-fake
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-features
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
      countryrisk:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-countryrisk
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/countryrisk
      volumerisk:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-volumerisk
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/volumerisk
      pepsanction-lookup-match-analyzer:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-pepsanction-lookup-match-analyzer
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction-lookup-match-analyzer
      simple:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-simple
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/simple
      assessment-engine:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-assessment-engine
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/assessment-engine
      passthrough:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-passthrough
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/passthrough
      smurfing:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-smurfing
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/smurfing
      roundamount:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-roundamount
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/roundamount
      round-amount-ratio-increase-decrease:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-round-amount-ratio-increase-decrease
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/round-amount-ratio-increase-decrease
      unusualfrequency:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-unusualfrequency
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/unusualfrequency
      dormantaccount:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-dormantaccount
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/dormantaccount
      crossbordervolume:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-crossbordervolume
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/crossbordervolume
      volumeincreasedecrease:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-volumeincreasedecrease
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/volumeincreasedecrease
      suspiciousavgticketsize:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-suspiciousavgticketsize
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/suspiciousavgticketsize
      account-based-frequency-increase-decrease:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-account-based-frequency-increase-decrease
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/account-based-frequency-increase-decrease
      volumeriskv2:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-volumeriskv2
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/volumeriskv2
      countryriskv2:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-countryriskv2
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/countryriskv2
      passthroughv2:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-passthroughv2
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/passthroughv2
      passthroughv3:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-passthroughv3
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/passthroughv3
      counterparty-volume:
        parameters:
          <<: *defaultParameters
          kustomize: github-fake
          java-version: 17
        ecr:
          <<: *defaultECR
          repository-prefix: hawk-features
          repository: hawk-features-counterparty-volume
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/counterparty-volume

  feature-enrichment:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-feature-enrichment
    kustomize:
      path: plattform/processing/feature-enrichment
      overlays: *defaultOverlays

  frontend:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket-fake
    ecr:
      <<: *defaultECR
      repository: hawk-hawkai-case-manager-frontend
    kustomize:
      path: plattform/case-manager/frontend
      overlays: *defaultOverlays

  label-engine:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-label-engine
    kustomize:
      path: plattform/processing/features/label-engine
      overlays: *defaultOverlays

  pep-sanction:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository-prefix: hawk-
    kustomize:
      overlays: *defaultOverlays
    modules:
      core:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
      rest:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-pepsanction-rest
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction
      msg:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-pepsanction-msg
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction
      import-dowjones:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-pepsanction-import-dowjones
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction
      import-info4c:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-pepsanction-import-info4c
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction
      import-lexis-nexis:
        parameters:
          <<: *defaultParameters
          kustomize: bitbucket
        ecr:
          <<: *defaultECR
          repository: hawk-hawkai-pepsanction-import-lexis-nexis
          repository-prefix: hawk-
        kustomize:
          overlays: *defaultOverlays
          path: plattform/processing/features/pepsanction

  processing-model:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket

  regulatory-check:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
      java-version: 17
    ecr:
      <<: *defaultECR
      repository: hawk-regulatory-check
    kustomize:
      path: plattform/processing/regulatory-check
      overlays: *defaultOverlays

  reprocessing:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-reprocessing
    kustomize:
      path: plattform/processing/reprocessing
      overlays: *defaultOverlays

  secure-storage:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-current-secure-storage
    kustomize:
      path: plattform/utility/secure-storage
      overlays: *defaultOverlays

  tenant-management:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-hawkai-tenant-management
      repository-prefix: hawk-hawkai-
    kustomize:
      path: plattform/utility/tenant-management
      overlays: *defaultOverlays

  transaction-announced-event-reducer:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-transaction-announced-event-reducer
    kustomize:
      path: plattform/processing/transaction-announced-event-reducer
      overlays: *defaultOverlays

  utils:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket

  watchtower:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket
    ecr:
      <<: *defaultECR
      repository: hawk-hawkai-watchtower
    kustomize:
      path: plattform/watchtower
      overlays: *defaultOverlays

  docker-images:
    parameters:
      <<: *defaultParameters
      kustomize: bitbucket-fake
    ecr:
      <<: *defaultECR














# End of file
